---
description: Deliver one bounded milestone with one writer and real acceptance
argument-hint: "[milestone]"
---
Implement ${@:-the next accepted milestone} within the existing objective.

- Inspect `workflow_contract` status and the current source. Reconcile genuine user corrections without removing unmet requirements. Confirm explicit writable roots, external inputs, success/failure journeys and artifact obligations before writing.
- Use one writer: either the parent for a tightly coupled small repair or one fresh Astra/max worker for a meaningful independent slice. Give the worker the goal, constraints, roots, relevant files, gates and expected artifact. Do not give workers arbitrary hard turn/tool caps that can strand a mutation. Keep independent read-only fanout bounded to two.
- Claim `writer_lease` before mutations and use exact one-use permits for shell/unknown effects. Drain and release before handing writing to another process. Mark delegated work as waiting, consume its result before depending on it, then explicitly resume actionable work. Parent-side overlap must not create another writer.
- Use Lens discovery and Hashline anchored edits. Preserve existing language/tooling; in Rust repositories, durable production, collection and experiment harnesses should be Rust rather than a new Python shortcut.
- Run focused diagnostics and canonical checks. For material changes, obtain one focused fresh review; fix concrete findings and resume that review once when useful instead of spawning duplicate review loops.
- If an authorized revision checkpoint changes HEAD, make it before final acceptance so receipts match the resulting revision. Run the actual pinned user journey and inspect artifacts yourself; child summaries and test counts are not evidence substitutes.
- Complete only through checked `workflow_contract` acceptance, then drain/release ownership. Report usage, changed paths/revision, executed checks, artifact locations and residual/deployment limits. Do not push, publish, activate a host or weaken scope without the corresponding authority.
